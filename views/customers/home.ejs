<%- include('../layouts/header.ejs') %>


    <!-- Page Preloder -->

     

   
    <!-- Humberger End -->

    <!-- Header Section Begin -->
    <%- include('../layouts/nav.ejs') %>
    
   
    <!-- Header Section End -->

    <!-- Hero Section Begin -->
    <section class="hero">
        <div class="container">
            <div class="row">
                <div class="col-lg-3">
                    <div class="hero__categories">
                        <div class="hero__categories__all">
                            <i class="fa fa-bars"></i>
                            <span>All departments</span>
                        </div>
                        <ul>
							<% if (categorys.length>0) {
								for(let i=0;i<categorys.length;i++) { %>
                                 
                            <li><a href="/category?id=<%= categorys[i]._id %>"><%= categorys[i].name %></a></li>
                            
                          
							<% } %>
							<% } else {%>
							  <tr>
								<td colspan="5">Product not found</td>
		  
							  </tr>
							 <% } %>
                            
                        </ul>
                    </div>
                </div>
                <div class="col-lg-9">
                    <div class="hero__search">
                        <div class="hero__search__form">
                            <form>
                                <div class="hero__search__categories">
                                    All Categories
                                    <span class="arrow_carrot"></span>
                                </div>
                                <input type="text" placeholder="What do yo u need?"name="search">
                                <button type="submit" class="site-btn">SEARCH</button>
                            </form>
                        </div>
                        <div class="hero__search__phone">
                            <div class="hero__search__phone__icon">
                                <i class="fa fa-phone"></i>
                            </div>
                            <div class="hero__search__phone__text">
                                <h5>+65 11.188.888</h5>
                                <span>support 24/7 time</span>
                            </div>
                        </div>
                    </div>
                    <div class="hero__item"  style="background-image: url('img/hero/banner.jpg');">
					
                        <div class="hero__text" >
							
                            <span>FRUIT FRESH</span>
                            <h2>Vegetable <br />100% Organic</h2>
                            <p>Free Pickup and Delivery Available</p>
                            <a href="/category?id=640afba8eee7eb14ec25c56d" class="primary-btn">SHOP NOW</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!-- Hero Section End -->

    <!-- Categories Section Begin -->
    <section class="categories">
        <div class="container">
            <div class="row">
                <div class="section-title">
                    <h2>Categories</h2>
                </div>
                <div class="categories__slider owl-carousel">
                  
                    <% if (categorys.length>0) {
                        for(let i=0;i<categorys.length;i++) { %>
                    

                    <div class="col-lg-3">
                        <div class="categories__item set-bg" >
                            <h5><a href="/category?id=<%= categorys[i]._id  %>"><%= categorys[i].name %></a></h5>
                             <td><img style="height:20rem;width:20rem;" src="/productimage/<%= categorys[i].Image %>" alt="<%= categorys[i].Image %>"></td>
                          
                        </div>
                    </div>

                   
							<% } %>
							<% } else {%>
							  <tr>
								<td colspan="5">Category not found</td>
		  
							  </tr>
							 <% } %>
                  
                </div>
            </div>
        </div>
    </section>
    <!-- Categories Section End -->

    <!-- Featured Section Begin -->
    <section class="featured spad">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    
                    <div class="featured__controls">
                        <ul>
                           
                         
                        </ul>
                    </div>
                </div>
            </div>
             <div class="banner">
        <div class="container">
            <% if (bannerData.length>0) { %>
                      
            
            <div class="row">
                <div class="col-lg-6 col-md-6 col-sm-6">
                    <div class="banner__pic">
                        <img src="/productimage/<%= bannerData[0].Image %>" alt="<%= bannerData[0].Image  %>">
                    </div>
                </div>
                <div class="col-lg-6 col-md-6 col-sm-6">
                    <div class="banner__pic">
                        <img src="/productimage/<%= bannerData[1].Image %>" alt="<%= bannerData[1].Image %>">

                    </div>
                </div>
            </div>
       
           
           <% } else {%>
             <tr>
               <td colspan="5">banner not found</td>

             </tr>
            <% } %>
        </div>
    </div>
    <div class="section-title">
        <h2 class="mt-5">Featured Product</h2>
    </div>
            <div class="row featured__filter">
                <% if ( product.length>0) {
                    for(let i=0;i<product.length;i++) {
                         %>
                <div class="col-lg-3 col-md-4 col-sm-6 <%= product[i].name %>">
                    <div class="featured__item">

                        <div class="featured__item__pic set-bg" >
                            <td><img style="height:16rem;width:20rem;" src="/productimage/<%= product[i].Image[0] %>" alt="<%= product[i].Image[0] %>"></td>

                            <ul class="featured__item__pic__hover">
                                <li> <a  onclick="addwishlist('<%= product[i]._id %>')"><i class="fa fa-heart"></i></a></li>
                                <li><a href="/view-product?id=<%= product[i]._id %>"><i class="fa fa-eye"></i></a></li>
                                <li><a onclick="addcart('<%= product[i]._id %>')"><i class="fa fa-shopping-cart"></i></a></li>
                            </ul>
                        </div>
                        <div class="featured__item__text">
                            <div class="product__discount__item__text">
                            <span><%= product[i].category.name %></span>
                            <h6><%= product[i].name %></h6>
                            
                            <div><h5>Rs.<%= product[i].price %>  <del class="text-muted fs-6 ms-1">Rs.<%= product[i].grossprice %></del></h5>
                            </div>
                            
                        </div>
                        </div>
                    </div>
                </div>
                <%  } %>
							<% } else {%>
							  <tr>
								<td colspan="5">Category not found</td>
		  
							  </tr>
							 <% } %>

                <div>
                    <br>
                    <% 
                    for(let j= 1 ;j <= totalPages; j++){
                        %>
                        <a href="?page=<%= j %>"><%= j %></a>
                        <%
                    }
                     %>
                     
            </div>
        </div>
        
    </section>
    <!-- Featured Section End -->

    <!-- Banner Begin -->
   
    <!-- Banner End -->

    

   

    <!-- Footer Section Begin -->
 
    <!-- Footer Section End -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <!-- Js Plugins -->
    <script>

        // add Wishlist
        function addwishlist(productId){
         $.ajax({
        
             url:`add-wishlist/${productId}`,
             method:'get',
             success:function(response){
                const Toast = Swal.mixin({
                                toast: true,
                                position: 'top',
                                showConfirmButton: false,
                                timer: 2000,
                                timerProgressBar: true,
                                didOpen: (toast) => {
                                    toast.addEventListener('mouseenter', Swal.stopTimer)
                                    toast.addEventListener('mouseleave', Swal.resumeTimer)
                                }
                                })

                                Toast.fire({
                                icon: 'success',
                                title: 'added to wishlist'
                                })
          
             }
         })
        }

        // add cart 
        function addcart(productId){
            $.ajax({
                url:`add-cart/${productId}`,
                method:'get',
             success:function(response){
                const Toast = Swal.mixin({
                                toast: true,
                                position: 'top',
                                showConfirmButton: false,
                                timer: 2000,
                                timerProgressBar: true,
                                didOpen: (toast) => {
                                    toast.addEventListener('mouseenter', Swal.stopTimer)
                                    toast.addEventListener('mouseleave', Swal.resumeTimer)
                                }
                                })

                                Toast.fire({
                                icon: 'success',
                                title: 'added to cart'
                                })
             }
                
            })
        }

        </script>



    <%- include('../layouts/footer.ejs') %>